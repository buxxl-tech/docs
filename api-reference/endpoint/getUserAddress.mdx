---
title: '获取用户地址'
openapi: 'POST /api/user/getUserAddress'
---

## 接口描述

获取用户存币地址，若该用户不存在，系统将自动注册用户 ID 并返回钱包地址。通常情况下，同一用户在同一链（chain）上使用不同的币种（symbol）时，获得的地址是相同的。因此，在创建数据库唯一索引时，请不要将地址包含在内，以便允许相同地址的存在。